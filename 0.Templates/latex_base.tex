\documentclass[a4paper, 12pt, twoside]{article}

% Lang
\usepackage[french]{babel}

% Pandoc requirements
$if(highlighting-macros)$
$highlighting-macros$
$endif$
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Inline code styling
\ifdefined\inlinecode\else
\newcommand{\inlinecode}[1]{\colorbox{lightwhite}{\texttt{#1}}}
\fi

% Existing template base
\input{latex_base.sty}

% Default image sizing
\setkeys{Gin}{width=\linewidth,totalheight=\textheight,keepaspectratio}

\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{calc}

\usepackage[toc, numberedsection=autolabel]{glossaries}
\makeglossaries

\usepackage[backend=biber, style=numeric]{biblatex}
\addbibresource{references.bib}

% Force images to be placed exactly where they are in the text (non-floating)
\usepackage{float}
\makeatletter
\def\fps@figure{H}
\makeatother

\newglossaryentry{mcp}{
    name=MCP,
    description={Model Context Protocol, an open standard that enables developers to build secure, two-way connections between their data sources and AI models}
}

% Title metadata
$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$author$}
$endif$
$if(date)$
\date{$date$}
$endif$

\begin{document}
    
    \thetitle{$if(subtitle)$$subtitle$$else$Rapport$endif$}{$title$}
    
    \vspace{10pt}
    \begin{flushleft}
    $if(source)$
    \small \textbf{Source:} \url{$source$} \\
    $endif$

    $if(tags)$
    \small \textbf{Tags:} $for(tags)$$tags$$sep$, $endfor$ \\
    $endif$

    $if(description)$
    \small \textbf{Description:} $description$
    $endif$
    \end{flushleft}
    \vspace{10pt}
    
    $if(toc)$
    \tableofcontents
    \newpage
    $endif$

    $body$

    \printglossaries

    \printbibliography[heading=bibnumbered]

\end{document}
